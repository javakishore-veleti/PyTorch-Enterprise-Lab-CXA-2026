"""Week 11 — Experiment Tracking & Canary Deployment.

Modules:
    mlflow_setup       — MLflow tracking URI & experiment creation
    experiment_logger  — Log params, metrics, artifacts per experiment
    model_registry     — Register and stage models in MLflow
    model_card         — Write model card documentation
    canary_strategy    — Canary deployment config & rollback runbook
"""
