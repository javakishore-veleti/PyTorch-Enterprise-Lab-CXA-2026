"""Week 02 — Training Loop & DataLoader.

Modules:
    dataset            — Custom map-style ComplaintDataset
    iterable_dataset   — Iterable-style streaming dataset
    dataloader         — DataLoader with pin_memory & num_workers
    classifier         — Simple MLP complaint classifier
    training_loop      — Full train/eval loop with seed control
    checkpointing      — Save & resume from checkpoint
"""
