"""Week 10 — Quantization, FastAPI Serving & Docker.

Modules:
    quantization  — INT8 dynamic quantization via ONNX Runtime
    serving       — FastAPI prediction endpoint
    middleware    — Correlation ID, timeout, structured logging
    load_test     — Concurrent load test with latency report
"""
