"""Week 12 — Drift Detection, Monitoring & ADR.

Modules:
    psi                 — Population Stability Index computation
    ks_drift            — KS-test per feature drift detection
    concept_drift       — Prediction distribution shift detection
    prometheus_metrics  — Prometheus text format metrics exporter
    audit_logger        — Structured JSONL audit logging
    adr_generator       — Architecture Decision Record generator
"""
