"""Week 00 — Dataset Setup & Validation.

Modules:
    config              — Dataset configs, paths, presence checks
    forex_data          — EUR/USD Forex parquet setup
    cfpb_data           — CFPB consumer complaints (HuggingFace)
    cic_iot_data        — CIC IoT 2023 network traffic
    cmapss_data         — NASA CMAPSS engine degradation
    oasst1_data         — OpenAssistant instruction pairs
    stackoverflow_data  — StackOverflow Java/ES Q&A
    __main__            — Orchestrator: run all, skip if present
"""
